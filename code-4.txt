main
num V_x, num V_y, text V_message,
begin
    V_x < input;
    V_x = not(V_x);
    if div(not(3), grt(3,4)) then
        begin
        end
    else
        begin
            V_y = mul(V_y, 2);
            print V_y;
        end;
    F_calculate(V_x, V_y, V_message);
end
num F_calculate(V_a, V_b, V_msg)
    {
        num V_t, num V_temp, text V_output,
        begin
        end
    }
    end
end