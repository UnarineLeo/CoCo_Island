main
num Vx, num V_y, text V_message,
begin
    V_x < input;
    = not(V_x);
    V_y = add(V_x, 5);
    V_message = "Hello";
end
num F_calculate(V_a, V_b, V_msg)
    {
        num V_t, num V_temp, text V_output,
        begin
            V_temp = add(V_a, V_b);
            V_t = div(V_temp, 2);
        end
    }
    end
end